import './navbar.css';

function Navbar() {
  return (
    <nav>
        <section>
            <a href="#">Home</a>
            <a href="#">About</a>
            <a href="#">Projects</a>
            <a href="#">Contact</a>
        </section>
    </nav>
  )
}

export default Navbar